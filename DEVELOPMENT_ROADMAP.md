# 开发线路

核对日期：2026-09-24。路线只描述交付顺序和验收门槛；功能定义、数据归属和协议字段分别以 [PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) 与 [protocol/protocol.md](protocol/protocol.md) 为准。

## 当前状态

### 阶段 A：模型收敛，已完成

- 标题、分栏、数据库、位置、媒体、Todo、引用和 Canvas 的唯一事实来源已确定。
- `[[...]]` 是默认关联格式；六点菜单只复制稳定块链接；实时引用需要明确选择显示方式。
- 普通块没有持久化的子块容器；Canvas 嵌套是打开关系，不改变目录父子关系。
- 旧字段保留明确的读取迁移入口，新代码不再写入旧结构。

### 阶段 B：新版浏览器能力，已交付并持续回归

当前交付入口是 `editor/` + `BrowserMockHost`，包括：

- 编辑、源码、预览三态，Markdown/GFM、HTML/CSS、表格、媒体和位置块。
- 块级拖拽、正文分栏、列宽调整、分栏恢复、对齐、注释、样式管理和历史撤销/重做。
- 跨笔记本双链、块/标题引用、实时引用显示方式、局部覆写、反向引用和来源跳转。
- Todo 创建/目标/完成日期、日历日记预览和目标日期状态点。
- 智能数据库、字段类型、公式/规则、关系、汇总、DQL 和 Markdown/CSV 导出入口。
- Canvas 文档/Canvas/正文块/媒体节点、右键菜单、联想引用、Icon/预览切换、字号、手绘、Bezier 曲线和画布历史。
- 地图管理、坐标输入/选点、位置块和统一媒体上传预览。

这些能力在当前阶段由页面内存 Mock 提供。浏览器刷新会重置 fixture，不能把通过新版页面测试理解成 SQLite 已落地。

### 阶段 C：稳定性收口，当前优先

先完成以下事项，再扩大功能范围：

1. 清理旧测试与当前 canonical 规则的冲突，保留覆盖新规则的回归用例。
2. 对 Canvas 曲线绘制、曲线弹窗、Todo 日历状态和日期显示补齐定向测试。
3. 对保存 ACK/NACK、mutation 幂等、版本冲突、历史恢复和刷新引用做连续操作验证。
4. 检查浏览器右栏在文档/Canvas/数据表/日历/地图之间的激活状态，不产生重复面板或第二套数据。
5. 完成视觉检查、`git diff --check` 和新版构建，才进入桌面接入。

### 阶段 D：新版接入真实宿主，后续

交付门槛是同一套 DTO 和命令贯通 `editor/desktop.ts`、`EditorHostController`、`INoteRepository` 和 SQLite：

- Canvas 的节点、视口、曲线和媒体地址进入正式存储模型，或明确迁移到现有 `views/placements/edges`。
- 数据库字段/记录、位置目录、样式、Todo 日期和引用实例具备真实事务、软删除、版本和恢复。
- 主窗口与便签都加载同一新版协议入口，关闭前等待 flush，跨进程重启可读回。
- 浏览器 Mock 与 SQLite 对相同命令返回一致的成功、错误、版本和归属语义。

在这一步完成前，不同步声称桌面端拥有新版 Canvas、地图、数据库或三态编辑器。

### 阶段 E：兼容路径退役，后续

- 将 `web/src` 的旧路由和新增能力逐步收敛到宿主协议。
- 保留明确的兼容迁移，删除重复的 DOM 注入和第二套状态。
- 桌面交互与新版测试覆盖一致后，再决定是否移除旧 `type` 消息。

### 阶段 F：长期能力，暂不排期

云同步、多人协作、离线冲突解决、跨窗口实时广播、词级格式合并、复杂查询和更多 Canvas 视图需要单独设计权限、冲突和增量同步协议。它们不属于当前 MVP 收口范围。

## 路线约束

- 新功能先复用现有块、引用、媒体、历史和宿主命令，不为同一概念再造 UI 状态或数据库。
- 先补 DTO、Mock、错误路径和测试，再扩展渲染。
- 只读预览、日历点、查询结果和引用投影不能触发源数据写入。
- 任何桌面完成声明都必须有真实 SQLite、关闭、重启和兼容入口证据。

